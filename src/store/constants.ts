/**
 * Constants are used for defining the names of functions and actions for handling state.
 * We define constants so that our function names are predictable (like our state) and it
 * results in less errors.
 *
 */

export const UPDATE_USER = 'updateUser';
export const LOAD_REPOS = 'loadRepos';
